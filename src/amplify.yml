version: 1
backend:
  phases:
    build:
      commands:
        - nvm install 20
        - nvm use 20
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm install
    build:
      commands:
        - npm run build
